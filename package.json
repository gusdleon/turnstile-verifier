{
	"name": "turnstile-verifier",
	"version": "0.1.0",
	"private": true,
	"devDependencies": {
		"wrangler": "2.9.1"
	},
	"scripts": {
		"deploy": "wrangler publish",
		"dev": "wrangler dev --local"
	}
}
